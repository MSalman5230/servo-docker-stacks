# warp

Docker Compose stack for `warp`.

## Required environment variables

- None

## Optional environment variables

- `WARP_SLEEP` (set to `2` in `compose.yaml`)
- `WARP_LICENSE_KEY` (optional, currently commented in `compose.yaml`)
