# camofox-browser

Docker Compose stack for `camofox-browser`.

## Required environment variables

- None

## Optional environment variables

- `CAMOFOX_PORT` (set to `9377` in `compose.yaml`)
- `MAX_OLD_SPACE_SIZE` (set to `256` in `compose.yaml`)
